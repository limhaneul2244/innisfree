import css from '../../styles/Button.module.scss';
/**
 * @param color
 * @param size
 * @param className
 * @returns 버튼
 */
const Button = ({className}) => {
    return (
        <></>
    )
}

export default Button;